# Fixture only. This service is never built; it exists so the indexer has real
# Rust syntax and an axum route table to parse.
[package]
name = "gateway-rs"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
